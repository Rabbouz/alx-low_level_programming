#include "main.h"

/**
 * _isupper - Writing a function that checks for uppercase character
 * @i: checked number
 * Return: 1 for uppercase / 0 otherwise
 *
*/

nt _isupper(int c)

{
	if (c == 'A')
	{
		return (1);
	}
	else if (c == 'a')
	{
		return (0);
	}
}
